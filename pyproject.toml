[project]
name = "devtul"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
"typer",
"pyyaml",
"gitpython"
]

[scripts]
devtul = "main:app"
